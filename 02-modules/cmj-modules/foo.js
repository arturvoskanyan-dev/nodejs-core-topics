export function sayHi(name) {
    return `Hello ${name}`
}

export default class Point {
    constructor(x, y) {
        this.x = x;
        this.y = y;
    }
}

// export async function foo(a, b) {
//     return await  a * b;
// }

